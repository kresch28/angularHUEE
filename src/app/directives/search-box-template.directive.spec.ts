import { SearchBoxTemplateDirective } from './search-box-template.directive';

/*describe('SearchBoxTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchBoxTemplateDirective();
    expect(directive).toBeTruthy();
  });
});*/

